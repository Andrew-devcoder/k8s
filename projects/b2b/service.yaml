apiVersion: v1
kind: Service
metadata:
  name: b2b
  namespace: b2b
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: b2b
  ports:
    - name: http
      port: 80
      targetPort: http
